
abbr -ag gd git diff
abbr -ag gitd git diff

abbr -ag gs git status -sb
abbr -ag gits git status -sb

abbr -ag dpr docker system prune

abbr -ag dcu docker-compose up --build -d
abbr -ag dcub docker-compose up --build
abbr -ag dcup docker-compose up -d

abbr -ag dcd docker-compose down
abbr -ag dcdown docker-compose down
abbr -ag dcdv docker-compose down --volumes

abbr -ag dcl docker-compose logs
abbr -ag dcr docker-compose run

